[package]
name = "seam"
version = "0.9.0"
edition = "2021"
description = "A tiny scripting language that borrows its batteries from Python and Node"
license = "MIT"
repository = "https://github.com/tedfiedler/seam"

[dependencies]
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
